const {
  putCategory, getCategory, getItems, getFeatures, destroyAllCategories,
} = require('./category.handler');

module.exports = {
  putCategory,
  getCategory,
  getItems,
  getFeatures,
  destroyAllCategories,
};
