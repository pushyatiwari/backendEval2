const {
  insertProductDetails, getAllCategories, getAllItems, destroyAll, getDistinctFeatures,
} = require('./category.services');

module.exports = {
  insertProductDetails,
  getAllCategories,
  getAllItems,
  destroyAll,
  getDistinctFeatures,
};
